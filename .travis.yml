language: ruby
cache: bundler

rvm:
  - 2.1.1
  - 2.0.0

script: 'bundle exec rake spec'

notifications:
  email:
    recipients:
      - eturino@eturino.com
    on_failure: change
    on_success: never